# Предложение: конвенция типизации optional-полей публичного API

## Одной строкой

Расхождение публичных типов `.d.ts`, JSDoc `@default` и рантайм-дефолтов — без единого правила.

---

## Проблема (зачем вообще)

У каждого настраиваемого поля компонента есть три «слоя», которые должны говорить одно и
то же:

- **тип** в публичном `.d.ts` — что пользователь имеет право присвоить полю;
- **`@default`** в JSDoc — какое значение по умолчанию показано в документации;
- **рантайм-дефолт** (`defaultOptions`) — что компонент реально подставляет, если поле не задано.

Единого правила, как их согласовывать, нет — и слои разъезжаются. Замер: **630**
несоответствий между ними (разбивка — в обосновании). Следствия:

- **Документация не везде отражает реальность:** `@default` в публичной API-доке берётся из JSDoc — где он разошёлся
  с рантаймом, пользователь читает неверное значение по умолчанию.
- **Тип не всегда верный** — он не отражает того, как ведёт себя компонент.
- **Нет правила для нового кода:** каждое новое поле заново решает «`null` или `undefined`».

## Прецеденты

1. [T1093403](https://isc.devexpress.com/internal/ticket/details/T1093403)
(I. Kharchenko, 2022): без `| undefined` в типе Angular-обёртка не давала присвоить полю
`undefined` — `[width]="undefined"`.
2. [T1327930](https://isc.devexpress.com/internal/ticket/details/T1327930)
3. [T1282901](https://isc.devexpress.com/internal/ticket/details/T1282901)

---

## Решение — конвенция + инструмент

1. **Конвенция** ([OPTIONAL_FIELDS_CONVENTION.md](OPTIONAL_FIELDS_CONVENTION.md)) — короткое
   механическое правило: как объявить optional-поле в типе и что писать в `@default`. Ответ
   **однозначно выводится из поведения компонента**. Обязательно для **нового** кода;
   существующее не ломаем (см. следующий раздел). Сами формулировки — по ссылке.

2. **Инструмент — главное.** Конвенцию **не нужно держать в голове** — её соблюдение
   проверяется автоматически, на двух уровнях:
   - **линтер** подсвечивает несоответствия типа и `@default` прямо в редакторе и на сборке;
     отдельная защита в CI не даёт числу нарушений расти;
   - **ревью с GitHub Copilot** (через файл инструкций `API_conventions.instructions.md`)
     ловит то, что линтер не может: выбор между `undefined` и `null` для нового поля.

   Это не «ещё одна конвенция, которую держат в голове», а инструмент, который **снижает
   нагрузку**: написал поле не так — сразу видишь подсказку.

   > Внесение правила в `API_conventions.instructions.md` (второй уровень, Copilot) —
   > следующий шаг, уже после согласования.

---

## Что это значит для публичного API

Коротко: **0 breaking changes.**

Строго по конвенции `null` стоило бы заменить на `undefined` почти везде — в подавляющем
большинстве мест он ничем не оправдан и остался лишь историческим артефактом. Но такая
замена **сломала бы** существующих пользователей: меняется и форма типа, и значение,
которое возвращает компонент. Поэтому для существующего кода мы её **не делаем**.
Существующие типы правим только безопасно — без изменений, ломающих чей-либо код. Правило
работает в полную силу на **новом** коде.

> Массовая замена `null → undefined` вне текущего предложения.

---

## Предложение

Сейчас необходимо: **согласовать конвенцию как общую для всех команд.** Дальше:

- правило и линтер уже готовы — отдельно «внедрять» нечего (есть PR);
- пилот на `chat.d.ts` показывает эталон оформления;
- каждая команда приводит свои поля в порядок в удобном темпе;
- защита в CI гарантирует, что новых нарушений не появится.
